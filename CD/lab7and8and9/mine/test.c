#include <stdio.h>
#include <stdlib.h>

int main()
{
	float car, bike;
	double helicopter;
	int plane, jet, r, turbo[7];
	car = bike;
	while (plane <= jet)
	{
		if (plane == jet)
		{
			for (r = 0; r <= 16; r = r + 2)
			{
				if (r == 4)
				{
					jet = plane;
				}
				plane = plane + 1;
			}
		}
	}
	return 0;
}